import { ScrollHandlerState } from './Scroll.types';
import SectionScrollHandler from './SectionScrollHandler';
export declare class ScrollManager {
    private scrollStack;
    private scrollHandlersMap;
    private scrollControl;
    private getScrollMove;
    private hasReachedEndOfDocument;
    private shouldParentResumeScrolling;
    private transferControlToChild;
    private transferControlToParent;
    private initializeScrollHandler;
    get activeScrollHandler(): SectionScrollHandler;
    shouldCedeControlToChld({ childs, currentChildIndex }: ScrollHandlerState): boolean;
    shouldCedeControlToParent({ childs, currentChildIndex, scrollState }: ScrollHandlerState): boolean;
    subscribe(container: HTMLDivElement, scrollUIState: ScrollHandlerState): SectionScrollHandler;
}
export declare const scrollManager: ScrollManager;
