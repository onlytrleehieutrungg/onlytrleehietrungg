import { ScrollControls, ScrollHandlerState } from './Scroll.types';
import { ScrollManager } from './ScrollManager';
declare class SectionScrollHandler {
    readonly container: HTMLDivElement;
    private scrollUIState;
    private scrollManager;
    private wheelScrollEvent;
    private touchScrollEvent;
    private lasHandledEventId;
    private skippedFirstTouchs;
    private bindedShouldHandleWheelScroll;
    private bindedShouldHandleTouchScroll;
    private bindedHandleResize;
    private lastTouchMouve;
    constructor(container: HTMLDivElement, scrollUIState: ScrollHandlerState, scrollManager: ScrollManager);
    private reinitializeScrollState;
    private handleResize;
    private attachScrollListener;
    private removeScrollListener;
    private blockScroll;
    private enableScroll;
    private captureOngoingWheelScrollState;
    private captureOngoingTouchScrollState;
    private setNextChild;
    private onScrollEnd;
    private shouldHandleWheelScrollEvent;
    private shouldHandleTouchScrollEvent;
    private handleScroll;
    private initializeScroll;
    private setScrollState;
    scrollTo(target: number): void;
    scroll(): void;
    init(scrollControl: ScrollControls): this;
    disable(): void;
    get UIScrollStateCopy(): ScrollHandlerState;
}
export default SectionScrollHandler;
